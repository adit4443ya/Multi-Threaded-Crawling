// finalize_json.h
#ifndef FINALIZE_JSON_H
#define FINALIZE_JSON_H

#include <filesystem>
#include <fstream>
#include <iostream>
#include <string>

void finalizeJsonFiles(const std::string &outputDir);

#endif // FINALIZE_JSON_H
